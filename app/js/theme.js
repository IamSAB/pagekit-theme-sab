import UIkit from 'uikit';
import Icons from 'uikit/dist/js/uikit-icons';
import Courthouse from '../../icons/courthouse.svg';

// loads the Icon plugin
UIkit.use(Icons);

UIkit.icon.add({
    courthouse: Courthouse
});